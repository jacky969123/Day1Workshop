# Snip Backend

A tiny URL shortener backend built with Bun.

## Run locally

```bash
bun install
bun start
```

The server listens on port 3000 by default and exposes the Snip API for creating short links and redirecting visitors.
